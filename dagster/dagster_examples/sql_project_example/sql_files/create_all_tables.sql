CREATE TABLE IF NOT EXISTS num_table (
    num1 INT,
    num2 INT
);

CREATE TABLE IF NOT EXISTS sum_table (
    num1 INT,
    num2 INT,
    sum INT
);

CREATE TABLE IF NOT EXISTS sum_sq_table (
    num1 INT,
    num2 INT,
    sum INT,
    sum_sq INT
);