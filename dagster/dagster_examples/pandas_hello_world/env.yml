environment:
  inputs:
    - input_name: num
      source: CSV
      args:
        path: "pandas_hello_world/num.csv"
    - input_name: sum
      source: CSV
      args:
        path: "sum.csv"

materializations:
  - solid: sum
    type: CSV
    args:
      path: 'sum.csv'
  - solid: sum_sq
    type: CSV
    args:
      path: 'sum_sq.csv'
