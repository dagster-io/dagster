[tox]
requires =
    tox-uv>=1
skipsdist = true

[testenv]
download = true
passenv =
    CI_*
    BUILDKITE*
    PYTEST_ADDOPTS
    PYTEST_PLUGINS
deps =
  -e ../../python_modules/dagster[test]
  -e ../../python_modules/dagster-pipes
  -e ../../python_modules/libraries/dagster-shared
  -e vendor/catalog_coach_runtime
  -e vendor/risk_reviewer_runtime
  -e vendor/briefing_writer_runtime
  -e .
allowlist_externals =
  /bin/bash
  uv
commands =
  !windows: /bin/bash -c '! uv pip list --exclude-editable | grep -e dagster'
  pytest -vv
