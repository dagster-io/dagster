[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.dagster]
module_name = "project_dagster_modal_pipes.definitions"
code_location_name = "project_dagster_modal_pipes"
