[project]
name = "shared"
version = "0.1.0"
requires-python = ">=3.10,<3.15"
description = "A shared package"
dependencies = [
    "dagster",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"