type: asset_check_suite_example.lib.AssetCheckSuite

attributes:
  asset: prepared
  checks:
    - type: is_between
      columns:
        - age
      min: 0
      max: 99
    - type: is_not_null
      columns:
        - user_id
        - username
        - age
        - city
