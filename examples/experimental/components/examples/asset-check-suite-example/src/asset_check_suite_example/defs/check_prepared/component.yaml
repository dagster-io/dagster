type: asset_check_suite_example.lib.AssetCheckSuite

attributes:
  asset: users_prepared
  checks:
    - type: is_between
      columns:
        - age
      min: 0
      max: 120

    - type: is_not_null
      columns:
        - user_id
        - username
        - age
        - city

    - type: has_unique_values
      columns:
        - user_id

    - type: matches_pattern
      pattern: email
      columns:
        - email
