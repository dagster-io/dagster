
solids:
  start:
    config:
      throw_in_solid: true
      return_wrong_type: false

  middle:
    config:
      throw_in_solid: false 
      return_wrong_type: false

  end:
    config:
      throw_in_solid: false 
      return_wrong_type: false

context:
  errorable_context:
    config:
      throw_on_context_init: false 

    resources:
      errorable_resource:
        config:
          throw_on_resource_init: false
