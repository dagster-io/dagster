resources:
  transporter:
    config:
      bucket_path: /tmp/test_bucket
solids:
  download_zipfiles_from_urls:
    inputs:
      base_url:
        value: http://foo.com
      file_names:
        - value: foo.zip
        - value: bar.zip
        - value: baz.zip
