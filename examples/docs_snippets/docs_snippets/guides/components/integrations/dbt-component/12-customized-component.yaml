type: dagster_dbt.DbtProjectComponent

attributes:
  project: '{{ load_ctx.project_root }}/dbt'
  select: "customers"
  translation:
    group_name: dbt_models
    description: "Transforms data using dbt model {{ node.name }}"
