[project]
name = "project_analytics"
version = "0.1.0"
description = "Dagster project for Pypi."
readme = "README.md"
requires-python = ">=3.8,<3.13"
dependencies = [
    "dagster",
    "dagster-cloud",
    "dagster-dbt",
    "dagster-duckdb-pandas",
    "dagster-gcp",
    "dagster-gcp-pandas",
    "dagster-hex",
    "dagster-pandas",
    "dbt-bigquery",
    "dbt-duckdb",
    "google-cloud-bigquery",
    "psycopg2-binary",
]

[project.optional-dependencies]
dev = ["dagster-webserver", "pytest"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.dagster]
module_name = "dagster_pypi.definitions"
code_location_name = "dagster_pypi"

[tool.ruff]

src = ["dagster_pypi"]

# Extend root configuration.
extend = "../../pyproject.toml"

[tool.ruff.lint]
# Use extend-ignore so that we ignore all the same codes ignored in root.
extend-ignore = [
  # (print found): Allow prints for demo purposes.
  "T201",

  # (relative imports): relative imports are acceptable in examples & docs
  "TID252"

]

[tool.setuptools.packages.find]
exclude = ["dagster_pypi_tests"]
