/**
 * Dagster+-only: where a code location's UI-authored components are committed.
 * Open source never commits components to a repo, so this renders nothing.
 */
export const AppManagedComponentRepoBindingSection = ({locationName: _}: {locationName: string}) =>
  null;
