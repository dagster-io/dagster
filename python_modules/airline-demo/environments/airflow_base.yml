context:
  airflow:
    resources:
      db_info:
        config:
          postgres_username: test
          postgres_password: test
          postgres_hostname: 127.0.0.1
          postgres_db_name: test
      filesystem:
        config:
          s3_bucket_name: dagster-airflow-scratch
          signed: false
