type: dagster.components.DefsFolderComponent

post_processing:
  processors:
    - target: "*"
      attributes:
        tags:
          another_level_tag: "true"
          env_tag: "{{ env('MY_ENV_VAR') }}"
