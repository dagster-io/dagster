# test that materializes some assets, then calls wipe_assets, then confirms that the ASSET_WIPED event is in the DB

# test that does the same as ^ but with a partitioned asset. Confirm that the wiped partitions are in the event
