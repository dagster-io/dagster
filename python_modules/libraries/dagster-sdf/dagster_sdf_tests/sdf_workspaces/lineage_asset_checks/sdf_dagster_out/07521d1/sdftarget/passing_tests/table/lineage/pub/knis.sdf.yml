table:
  name: lineage.pub.knis
  dialect: trino
  materialization: view
  purpose: model
  dependencies:
  - lineage.pub.middle
  columns:
  - name: txn_date
    datatype: varchar
    lineage:
      copy:
      - lineage.pub.middle.txn_date
  - name: qty
    datatype: bigint
    lineage:
      modify:
      - lineage.pub.middle.qty
  lineage:
    scan:
    - lineage.pub.middle.txn_date
  source-locations:
  - path: models/knis.sql
