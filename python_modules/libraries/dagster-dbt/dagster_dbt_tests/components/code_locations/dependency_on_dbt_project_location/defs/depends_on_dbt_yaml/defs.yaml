type: dagster.FunctionComponent

attributes:
  execution:
    name: downstream_of_customers_two
    fn: .execute.execute_fn
  assets:
    - key: downstream_of_customers_two
      deps:
        - "{{ context.component_tree.load_component_at_path('jaffle_shop_dbt').get_asset_key_for_model(context, 'customers') }}"
