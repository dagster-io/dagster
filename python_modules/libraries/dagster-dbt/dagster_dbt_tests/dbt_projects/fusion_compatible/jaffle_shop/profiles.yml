jaffle_shop:
  target: snowflake
  outputs:
    snowflake:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}"
      user: BUILDKITE
      private_key: "{{ env_var('SNOWFLAKE_BUILDKITE_PRIVATE_KEY') }}"
      client_session_keep_alive: False
      database: TESTDB
      schema: TESTSCHEMA
      warehouse: BUILDKITE
