source: LOCAL
target: DUCKDB

defaults:
  mode: full-refresh
  object: "{stream_table}"

streams:
  <PLACEHOLDER>:
    object: "main.tbl"
    meta:
      dagster:
        asset_key: singular_input_duckdb
        deps: [singular_input_csv]
