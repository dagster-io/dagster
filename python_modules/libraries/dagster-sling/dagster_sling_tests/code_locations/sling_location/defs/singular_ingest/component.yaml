type: dagster_sling.SlingReplicationComponent

attributes:
  replication:
    path: "./replication.yaml"
    asset_attributes:
      key: "singular/{{ stream_definition.config.meta.dagster.asset_key }}"
  sling:
    connections:
      - name: DUCKDB
        type: duckdb
        instance: <PLACEHOLDER>
        password: "{{ env('SOME_PASSWORD') }}"
