[build-system]
requires = [
    "hatchling>=1.19.0",
]
build-backend = "hatchling.build"

[project]
name = "dagster-k8s"
version = "0.29.5"
description = "A Dagster integration for k8s"
authors = [
    { name = "Dagster Labs", email = "hello@dagsterlabs.com" },
]
license = "Apache-2.0"
requires-python = ">=3.10,<3.15"
classifiers = [
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Operating System :: OS Independent",
]
dependencies = [
    "dagster==1.13.5",
    "kubernetes<37",
    "google-auth!=2.23.1",
]

[project.urls]
Homepage = "https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-k8s"

[dependency-groups]
test = [
    "dagster[test]",
    "dagster-pipes",
    "dagster-shared",
    "dagster-graphql",
    "dagster-test",
    "dagster-aws",
    "dagster-pandas",
    "dagster-gcp",
    "dagster-celery",
    "dagster-postgres",
    "dagster-celery-k8s",
    "dagster-celery-docker",
]
test-old-kubernetes = [
    "kubernetes==12.0.0",
]

[tool.uv.sources.dagster]
path = "../../dagster"
editable = true

[tool.uv.sources.dagster-pipes]
path = "../../dagster-pipes"
editable = true

[tool.uv.sources.dagster-shared]
path = "../../libraries/dagster-shared"
editable = true

[tool.uv.sources.dagster-graphql]
path = "../../dagster-graphql"
editable = true

[tool.uv.sources.dagster-test]
path = "../../dagster-test"
editable = true

[tool.uv.sources.dagster-aws]
path = "../../libraries/dagster-aws"
editable = true

[tool.uv.sources.dagster-pandas]
path = "../../libraries/dagster-pandas"
editable = true

[tool.uv.sources.dagster-gcp]
path = "../../libraries/dagster-gcp"
editable = true

[tool.uv.sources.dagster-celery]
path = "../../libraries/dagster-celery"
editable = true

[tool.uv.sources.dagster-postgres]
path = "../../libraries/dagster-postgres"
editable = true

[tool.uv.sources.dagster-celery-k8s]
path = "../../libraries/dagster-celery-k8s"
editable = true

[tool.uv.sources.dagster-celery-docker]
path = "../../libraries/dagster-celery-docker"
editable = true
