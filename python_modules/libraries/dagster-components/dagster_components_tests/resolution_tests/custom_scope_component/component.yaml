type: dagster_components_tests.resolution_tests.custom_scope_component.component.HasCustomScope

attributes:
  asset_attributes:
    group_name: "{{ custom_str }}"
    tags: "{{ custom_dict }}"
    metadata:
      prefixed: "prefixed_{{ custom_fn('a', custom_str) }}"
    automation_condition: "{{ custom_automation_condition('@daily') }}"
