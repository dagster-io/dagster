[build-system]
requires = ["hatchling>=1.19.0"]
build-backend = "hatchling.build"

[project]
name = "dagster-duckdb-polars"
version = "1!0+dev"
description = "Package for storing Polars DataFrames in DuckDB."
authors = [{name = "Dagster Labs", email = "hello@dagsterlabs.com"}]
license = "Apache-2.0"
requires-python = ">=3.10,<3.15"
dependencies = [
    "dagster==1!0+dev",
    "dagster-duckdb==1!0+dev",
    "polars[pyarrow]",
]

[project.urls]
Homepage = "https://github.com/dagster-io/dagster/tree/master/python_modules/libraries/dagster-duckdb-polars"

[dependency-groups]
test = [
    "dagster[mypy,test]",
    "dagster-pipes",
    "dagster-shared",
    "dagster-duckdb",
]

[tool.uv.sources]
dagster = { path = "../../dagster", editable = true }
dagster-pipes = { path = "../../dagster-pipes", editable = true }
dagster-shared = { path = "../dagster-shared", editable = true }
dagster-duckdb = { path = "../dagster-duckdb", editable = true }
