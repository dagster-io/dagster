import Typography from "typography";

const typography = new Typography({
  baseFontSize: "18px",
  baseLineHeight: 1.666,
  headerFontFamily: ["Inter", "Work Sans", "Helvetica Neue", "sans-serif"],
  bodyFontFamily: ["Inter", "Source Sans Pro", "serif"]
});

export default typography;
