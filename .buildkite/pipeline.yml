steps:
  - label: ":test_tube: Run Tests"
    command: |
      pip install -e python_modules/dagster
      pip install pytest loguru python-dotenv
      pytest -v python_modules/dagster/dagster_tests/logging_tests/
    env:
      BUILDKITE: "true"
    agents:
      queue: "public-default"
