steps:
  - label: ":test_tube: Run tests"
    command:
      - pip install -e .
      - pytest -v
    agents:
      queue: "linux_ci_small"
    env:
      BUILDKITE_FOLDER_GIT_REPO_PATH: "git@github.com:albertfast/buildkite-folder.git"
